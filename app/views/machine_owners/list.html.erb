<%= render(:partial => 'shared/flash_show') %>
<table class="table table-bordered table-hover">
<caption class="titlu-tabel">Machine owners list</caption>
<thead>
    <tr>
      <th>#</th>
      <th>Name</th>
      <th>Address</th>
      <th>Office tel</th>
      <th>Office mail</th>
      <th>Last updated</th>
    </tr>
</thead>
<tbody>
	<% @i=0 %>
	<% @owners.each do |owner| %>
    <tr>
      <td><%= @i+=1 %></td>
      <td><%= link_to(owner.name, {:action =>'show', :id => owner.id}) %></td>
      <td><%= owner.address %></td>
      <td><%= owner.office_tel %></td>
      <td><%= owner.office_mail %></td>
      <td><%= owner.updated_at.strftime("%d.%m.%Y at %H:%M") %></td>
      <% end %>
    </tr>
</tbody>
</table>
<%= render(:partial => "shared/add_button") %>