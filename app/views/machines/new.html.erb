<%= render(:partial => "shared/btn_back_to_list.html.erb") %>
<h1>New Machine</h1>
<%= form_for [@machine_group, @machine], class: "form-horizontal" do |f| %>
	<%= render 'form', :f => f %>
<% end %>

<div id="new_owner" class="modal hide" tabindex="-1" role="dialog" aria-labelledby="nw" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="machine_owner_model_header">New machine owner</h3>
  </div>
  <div class="modal-body" id="new_owner_modal_body">
  	<p>test</p>
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
    <button id="save_owner_btn" class="btn btn-primary">Save owner</button>
  </div>
</div>