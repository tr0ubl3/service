<h1><%= @user.full_name %> details</h1>
<div class="btn-group">
	<a href="#" class="btn dropdown-toggle" data-toggle="dropdown">
		User approval
		<span class="caret"></span>
	</a>
	<ul class="dropdown-menu">
		<li><%= link_to 'Approve registration', approve_user_path(@user) %></li>
		<li><a href="#">Decline registration</a></li>
	</ul>

</div>
<dl class="dl-horizontal">
	<dt>First name</dt>
	<dd><%= @user.first_name %></dd>
	<dt>Last name</dt>
	<dd><%= @user.last_name %></dd>
	<dt>Phone number</dt>
	<dd><%= @user.phone_number %></dd>
	<dt>Email</dt>
	<dd><%= @user.email %></dd>
	<dt>Firm</dt>
	<dd><%= @user.machine_owner_id %></dd>
	<dt>Registered at</dt>
	<dd><%= @user.created_at %></dd>
</dl>