<table cellpadding="0" cellspacing="0" border="0" id="general_events" class="table table-striped table-bordered">
	<thead>
		<tr>
			<th>#</th>
			<th>Event name</th>
			<th>Event ID</th>
			<th>Created by</th>
			<th>Status</th>
		</tr>
	</thead>
	<tbody>
		<% event_listing.each do |t| %>
			<tr>
				<td></td>
					<td><%= link_to service_event_path(t.id) do %>
							<%= t.event_name %>
						<% end %>
					</td>
				<td><%= date_format(t.event_date) %></td>
				<td><%= t.user.full_name %></td>
				<td><%= t.current_state %></td>
			</tr>
		<% end %>
	</tbody>
</table>