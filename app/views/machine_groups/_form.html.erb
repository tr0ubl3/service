<%= form_for(@machine_group) do |f| %>
  <% if @machine_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_group.errors.count, "error") %> prohibited this machine_group from being saved:</h2>

      <ul>
      <% @machine_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <label class="control-label">
      <%= f.label :manufacturer_id %>
    </label>
    <div class="controls">
      <%= f.collection_select(:manufacturer_id, Manufacturer.all, :id, :name) %>
    </div>
  </div>

  <div class="control-group">
    <label class="control-label">
      <%= f.label :machining_type %>
    </label>
    <div class="controls">
      <%= f.text_field :machining_type %>
    </div>
  </div>
  
  <div class="control-group">
    <label class="control-label">
      <%= f.label :machine_type %>
    </label>
    <div class="controls">
      <%= f.text_field :machine_type %>
    </div>
  </div>
  
  <div class="control-group">
    <label class="control-label">
      <%= f.label :version %>
    </label>
    <div class="controls">
      <%= f.text_field :version %>
    </div>
  </div>

  <div class="controls">
    <%= submit_tag("Save", :class => 'btn btn-success') %>
  </div>
<% end %>
