<%= form_for(@machine_group) do |f| %>
  <% if @machine_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_group.errors.count, "error") %> prohibited this machine_group from being saved:</h2>

      <ul>
      <% @machine_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :manufacturer_id %><br />
    <%= f.number_field :manufacturer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
